# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'YourProjectName' do
    use_frameworks!
    use_react_native!(
      :path => '../node_modules/react-native',
      # Specify whether to enable Hermes or not
      :hermes_enabled => true
    )
    
    # Add any additional Pods your project requires here
  
    target 'YourProjectNameTests' do
      inherit! :complete
      # Pods for testing
    end
  
  end
  